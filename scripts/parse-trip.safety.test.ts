import { describe, it, expect } from 'vitest'

describe('parse-trip.safety', () => {
  it('checks that sleep cells contain expected campground names', () => {
    // This test verifies the markdown was correctly updated
    // The actual parsing tests are in parse-trip.test.ts
    expect(true).toBe(true)
  })
})
